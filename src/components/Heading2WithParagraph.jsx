export default function Heading2WithParagraph({heading, text}) {
    return(
        <div className="not-hero">
            <h2>{heading}</h2>
            <p>{text}</p>
        </div>        
    );
}